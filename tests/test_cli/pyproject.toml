[tool.statisfactory]
project_slug = "exemple"
configuration = "conf"
catalog = "catalog.yaml"
sources = "Lib"
pipelines_definitions = 'pipelines/definitions'
parameters = 'pipelines/parameters'

[tool.statisfactory.entrypoints] 
module = 'side'